package com.uap.control_tickets.enums;

/**
 * Tipo de evento de acceso registrado al escanear el QR de un ticket.
 * ENTRADA marca que la persona esta dentro; SALIDA que salio.
 */
public enum TipoAcceso {
    ENTRADA,
    SALIDA
}
